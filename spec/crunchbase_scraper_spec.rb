require "spec_helper"

describe CrunchbaseScraper do
  it "has a version number" do
    expect(CrunchbaseScraper::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
